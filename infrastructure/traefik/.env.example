# Traefik Configuration Example
# Copy to .env and update with your actual values
# NEVER commit .env file to git

# Let's Encrypt Configuration
LETSENCRYPT_EMAIL=admin@visiquate.com

# Authentik Configuration
# Get AUTHENTIK_OUTPOST_TOKEN from: https://auth.visiquate.com/admin/ > System > Outposts
AUTHENTIK_BASE_URL=https://auth.visiquate.com
AUTHENTIK_OUTPOST_TOKEN=eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...

# Release Storage Configuration
# RELEASES_DIR: Local Docker volume mount path (default: /data/releases)
RELEASES_DIR=/data/releases

# UPLOAD_API_KEY: Secure key for release uploads via API
# Generate with: openssl rand -base64 32
UPLOAD_API_KEY=your-secure-random-key-change-this

# API Configuration
ENVIRONMENT=production
LOG_LEVEL=info

# Docker Image for Releases API
CCO_API_IMAGE=ghcr.io/visiquate/cco-releases-api:latest
