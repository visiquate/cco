{
  "name": "claude-orchestra",
  "version": "1.0.0",
  "description": "Multi-agent development orchestra with specialized coding agents coordinated by a conductor",
  "main": "src/orchestra-conductor.js",
  "scripts": {
    "orchestra": "node src/orchestra-conductor.js",
    "credentials": "node src/credential-manager.js",
    "sync": "node scripts/sync-orchestra-config.js",
    "init": "npm run setup:mcp",
    "setup:mcp": "echo 'MCP servers should be enabled in .claude/settings.local.json'",
    "test": "echo 'Test suite not yet implemented'",
    "help": "node -e \"console.log(require('fs').readFileSync('docs/ORCHESTRA_USAGE_GUIDE.md', 'utf8'))\""
  },
  "keywords": [
    "claude",
    "multi-agent",
    "ai",
    "development",
    "orchestration",
    "mcp"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=16.0.0"
  },
  "dependencies": {
    "eventsource": "^4.0.0",
    "playwright": "^1.56.1",
    "vectordb": "^0.21.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/claude-orchestra.git"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
