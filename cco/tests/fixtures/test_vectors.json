{
  "description": "Pre-computed test vectors for known text samples",
  "embedding_dimensions": 384,
  "normalization_range": [-1.0, 1.0],
  "algorithm": "SHA256-based deterministic embedding",
  "test_cases": [
    {
      "text": "Hello world",
      "expected_length": 384,
      "expected_first_values": [-0.01, 0.45, -0.67],
      "note": "First 3 values shown as example"
    },
    {
      "text": "FastAPI REST API",
      "expected_length": 384,
      "similarity_to": {
        "REST API framework": 0.85,
        "Database design": 0.32
      }
    },
    {
      "text": "JWT authentication security",
      "expected_length": 384,
      "similarity_to": {
        "authentication tokens": 0.90,
        "Docker containers": 0.15
      }
    }
  ],
  "edge_cases": [
    {
      "description": "Empty string",
      "text": "",
      "expected_behavior": "Should generate 384-dim vector with all zeros or reject"
    },
    {
      "description": "Single character",
      "text": "x",
      "expected_behavior": "Should generate valid 384-dim vector"
    },
    {
      "description": "Very long text (10KB)",
      "text_length": 10000,
      "expected_behavior": "Should hash consistently to 384-dim vector"
    },
    {
      "description": "Unicode and emoji",
      "text": "Hello 世界 🚀",
      "expected_behavior": "Should handle UTF-8 properly"
    }
  ]
}
