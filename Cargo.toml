[package]
name = "cco"
version = "2025.11.30"  # Date-based version: YYYY.MM.DD (git commit hash used for multi-release disambiguation)
edition = "2021"

[package.metadata]
# Actual CCO version using automatic date-based format: YYYY.MM.DD+commithash
# Version is automatically generated at build time (see build.rs)
# Example: 2025.11.17+e37b7c0
display_version = "auto"

[[bin]]
name = "cco"
path = "src/main.rs"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
tower = { version = "0.4", features = ["util"] }
tower-http = { version = "0.5", features = ["cors", "trace"] }
tokio = { version = "1.35", features = ["full"] }
tokio-stream = "0.1"
async-trait = "0.1"
async-stream = "0.3"
futures = "0.3"
moka = { version = "0.12", features = ["future"] }
reqwest = { version = "0.11", features = ["json", "rustls-tls", "stream"] }
reqwest-eventsource = "0.4"
sqlx = { version = "0.7", features = ["runtime-tokio-rustls", "sqlite"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
config = "0.13"
sha2 = "0.10"
hex = "0.4"
dirs = "5.0"
semver = "1.0"
toml = "0.8"
tar = "0.4"
flate2 = "1.0"
regex = "1.10"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
tracing-appender = "0.2"
thiserror = "1.0"
anyhow = "1.0"
chrono = { version = "0.4", features = ["serde"] }
prometheus = "0.13"
clap = { version = "4.4", features = ["derive"] }
uuid = { version = "1.6", features = ["v4", "serde"] }
sysinfo = "0.30"
portable-pty = "0.8"
dashmap = "5.5"
ratatui = { version = "0.26", features = ["serde"] }
crossterm = { version = "0.27", features = ["serde"] }
which = "5.0"
jsonwebtoken = "9.3"
secrecy = "0.8"
aes-gcm = "0.10"
pbkdf2 = "0.12"
base64 = "0.21"
zeroize = "1.7"
keyring = "2.3"
url = "2.5"
getrandom = "0.2"
parking_lot = "0.12"
notify = "6.1"

# LLM inference engine (Phase 1B) - migrated to mistral.rs for Qwen2 support
mistralrs = { git = "https://github.com/EricLBuehler/mistral.rs", branch = "master", features = ["metal"] }
indicatif = "0.17"
rand = "0.8"
num_cpus = "1.16"

# LanceDB for embedded vector database (Issue #32)
# Note: Arrow versions must match LanceDB's dependencies (v56.2.0)
lancedb = "0.22"
arrow-array = "56.2"
arrow-schema = "56.2"

[target.'cfg(unix)'.dependencies]
nix = { version = "0.27", features = ["signal"] }
libc = "0.2"

[build-dependencies]
include_dir = "0.7"
chrono = { version = "0.4", features = ["serde"] }
serde_json = "1.0"

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3.8"

# WASM dependencies
[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
web-sys = { version = "0.3", features = [
    "console",
    "Document",
    "Element",
    "HtmlCanvasElement",
    "CanvasRenderingContext2d",
    "Window",
    "Performance"
]}
js-sys = "0.3"
vte = { version = "0.13", default-features = false }
console_error_panic_hook = "0.1"
getrandom = { version = "0.2", features = ["js"] }

# Library configuration
[lib]
crate-type = ["rlib"]
path = "src/lib.rs"
name = "cco"

# WASM optimization profile
[profile.wasm-release]
inherits = "release"
opt-level = "s"     # Optimize for size
lto = true          # Link-time optimization
codegen-units = 1   # Single codegen unit for better optimization
